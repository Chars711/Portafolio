/* Estilos generales */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: #f2f2f2;
    color: #333;
}

header {
    background-color: #0077cc;
    color: white;
    padding: 2rem;
    text-align: center;
}

section {
    padding: 2rem;
}

/* Sección de Tarjetas */
.tarjetas .contenedor-tarjetas {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    gap: 1rem;
}

.tarjeta {
    background-color: white;
    padding: 1rem;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    width: 30%;
    min-width: 250px;
    text-align: center;
}

/* Sección de Contacto */
.contacto form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    max-width: 500px;
    margin: auto;
}

.contacto input,
.contacto textarea {
    padding: 0.5rem;
    border: 1px solid #ccc;
    border-radius: 4px;
}

.contacto button {
    background-color: #0077cc;
    color: white;
    padding: 0.75rem;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

footer {
    text-align: center;
    padding: 1rem;
    background-color: #004d80;
    color: white;
}